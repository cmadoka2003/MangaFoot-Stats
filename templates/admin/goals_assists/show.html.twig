<!DOCTYPE html>

<title>GoalsAssists</title>

{% block body %}
    <h1>GoalsAssists</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ goals_assist.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_goals_assists_index') }}">back to list</a>

    <a href="{{ path('app_goals_assists_edit', {'id': goals_assist.id}) }}">edit</a>

    {{ include('admin/goals_assists/_delete_form.html.twig') }}
{% endblock %}
