<!DOCTYPE html>

<title>Edit Overalls</title>

{% block body %}
    <h1>Edit Overalls</h1>

    {{ include('admin/overalls/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_overalls_index') }}">back to list</a>

    {{ include('admin/overalls/_delete_form.html.twig') }}
{% endblock %}
