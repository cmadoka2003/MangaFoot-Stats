<!DOCTYPE html>

<title>New Overalls</title>

{% block body %}
    <h1>Create new Overalls</h1>

    {{ include('admin/overalls/_form.html.twig') }}

    <a href="{{ path('app_overalls_index') }}">back to list</a>
{% endblock %}
