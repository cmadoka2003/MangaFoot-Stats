<!DOCTYPE html>

<title>New Favorite</title>

{% block body %}
    <h1>Create new Favorite</h1>

    {{ include('admin/favorite/_form.html.twig') }}

    <a href="{{ path('app_favorite_index') }}">back to list</a>
{% endblock %}
