<!DOCTYPE html>

<title>Edit Favorite</title>

{% block body %}
    <h1>Edit Favorite</h1>

    {{ include('admin/favorite/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_favorite_index') }}">back to list</a>

    {{ include('admin/favorite/_delete_form.html.twig') }}
{% endblock %}
