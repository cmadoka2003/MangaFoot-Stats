<!DOCTYPE html>

<title>Edit Teams</title>

{% block body %}
    <h1>Edit Teams</h1>

    {{ include('admin/teams/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_teams_index') }}">back to list</a>

    {{ include('admin/teams/_delete_form.html.twig') }}
{% endblock %}
