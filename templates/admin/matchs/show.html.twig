<!DOCTYPE html>

<title>Matchs</title>

{% block body %}
    <h1>Matchs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ match.id }}</td>
            </tr>
            <tr>
                <th>TeamHome</th>
                <td>{{ match.teamHomeId.name }}</td>
            </tr>
            <tr>
                <th>TeamAway</th>
                <td>{{ match.teamAwayId.name }}</td>
            </tr>
            <tr>
                <th>TeamHomeScore</th>
                <td>{{ match.teamHomeScore }}</td>
            </tr>
            <tr>
                <th>TeamAwayScore</th>
                <td>{{ match.teamAwayScore }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_matchs_index') }}">back to list</a>

    <a href="{{ path('app_matchs_edit', {'id': match.id}) }}">edit</a>

    {{ include('admin/matchs/_delete_form.html.twig') }}
{% endblock %}
